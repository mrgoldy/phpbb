services:
    dbal.conn:
        synthetic: true

    dbal.tools:
        class: phpbb\db\tools
        arguments:
            - '@dbal.conn'

    dbal.extractor:
        class: phpbb\db\extractor
        arguments:
            - '@dbal.conn'
            - '@filesystem.temp'
            - '@request'
