services:
# ----- Migrator -----
    migrator:
        class: phpbb\db\migrator
        arguments:
            - '@config'
            - '@service_container'
            - '@dbal.conn'
            - '@dbal.tools'
            - '@dispatcher'
            - '@migrator.helper'
            - '%tables.migrations%'
            - '%core.table_prefix%'
            - '%core.root_path%'
            - '%core.php_ext%'
            - '@migrator.tool_collection'

    migrator.helper:
        class: phpbb\db\migration\helper\helper

# ----- Migrator's tools -----
    migrator.tool_collection:
        class: phpbb\di\service_collection
        arguments:
            - '@service_container'
        tags:
            - { name: service_collection, tag: migrator.tool }

    migrator.tool.config:
        class: phpbb\db\migration\tool\config
        arguments:
            - '@config'
        tags:
            - { name: migrator.tool }

    migrator.tool.config_text:
        class: phpbb\db\migration\tool\config_text
        arguments:
            - '@config_text'
        tags:
            - { name: migrator.tool }

    migrator.tool.module:
        class: phpbb\db\migration\tool\module
        arguments:
            - '@dbal.conn'
            - '@language'
            - '@log'
            - '@module.manager'
            - '@user'
            - '%tables.modules%'
        tags:
            - { name: migrator.tool }

    migrator.tool.permission:
        class: phpbb\db\migration\tool\permission
        arguments:
            - '@auth'
            - '@cache.driver'
            - '@dbal.conn'
            - '%tables.acl_groups%'
            - '%tables.acl_options%'
            - '%tables.acl_roles%'
            - '%tables.acl_roles_data%'
            - '%tables.acl_users%'
            - '%core.root_path%'
            - '%core.php_ext%'
        tags:
            - { name: migrator.tool }
